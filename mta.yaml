ID: XSA_TEST2
_schema-version: '2.0'
version: 0.0.1

modules:
 - name: xsa_db2
   type: hdb
   path: xsa_db2
   requires:
    - name: hdi_xsa_db2

resources:
 - name: hdi_xsa_db2
   parameters:
      config:
         schema: PSW_XSA_2
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
